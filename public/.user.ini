; PHP settings for large Takeout uploads (watch-history.html ~68MB)
; Many shared hosts (cPanel, etc.) respect .user.ini in the document root
upload_max_filesize = 100M
post_max_size = 100M
memory_limit = 256M
max_execution_time = 300
